TODO: Each model like heartbeats, events, users, etc. will have a manager for easy interaction with DB, then with these add a callback for when they are changed to update wrapper for example (in case of rules)